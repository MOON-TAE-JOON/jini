# jini

Python scripts for generating the project's MIDI stems.

## MIDI output
Run the instrument scripts (e.g., `python Bass.py`, `python Organ.py`) to regenerate the corresponding `.mid` files locally. Binary MIDI outputs are not tracked in the repository to keep pull requests text-only.
